# Спека этапа 2 — PaymentCountdown
- Пропс: `remaining: number` (секунды).
- Разметка: `<strong className="countdown">` с текстом времени или сообщением об истечении.
- Стейты: активный (mm:ss), истёкший (текст из локали).
