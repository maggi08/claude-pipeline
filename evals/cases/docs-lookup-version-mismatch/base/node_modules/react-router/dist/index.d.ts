/**
 * react-router v6.22.3 — type declarations (trimmed)
 */
import * as React from "react";
export interface Path {
    pathname: string;
    search: string;
    hash: string;
}
export type To = string | Partial<Path>;
export type RelativeRoutingType = "route" | "path";
export interface NavigateOptions {
    replace?: boolean;
    state?: any;
    preventScrollReset?: boolean;
    relative?: RelativeRoutingType;
}
export interface NavigateFunction {
    (to: To, options?: NavigateOptions): void;
    (delta: number): void;
}
/**
 * Returns an imperative method for changing the location. Used by `<Link>`s, but
 * may also be used by other elements to change the location.
 */
export declare function useNavigate(): NavigateFunction;
export interface NavigateProps {
    to: To;
    replace?: boolean;
    state?: any;
    relative?: RelativeRoutingType;
}
/**
 * Changes the current location when it is rendered.
 */
export declare function Navigate({ to, replace, state, relative }: NavigateProps): null;
export interface RoutesProps {
    children?: React.ReactNode;
    location?: Partial<Path> | string;
}
/**
 * A container for a nested tree of `<Route>` elements that renders the branch
 * that best matches the current location.
 */
export declare function Routes({ children, location }: RoutesProps): React.ReactElement | null;
export interface RouteProps {
    path?: string;
    index?: boolean;
    element?: React.ReactNode | null;
    children?: React.ReactNode;
    caseSensitive?: boolean;
}
/** Declares an element that should be rendered at a certain URL path. */
export declare function Route(_props: RouteProps): React.ReactElement | null;
/** Renders the child route's element, if there is one. */
export declare function Outlet(props: { context?: unknown }): React.ReactElement | null;
/** Returns the current location object, which represents the current URL in web browsers. */
export declare function useLocation(): Path & { state: unknown; key: string };
