/**
 * react-router-dom v6.22.3 — type declarations (trimmed)
 */
import * as React from "react";
import type { To } from "react-router";
export { Navigate, Outlet, Route, Routes, useLocation, useNavigate } from "react-router";
export type { NavigateFunction, NavigateOptions, NavigateProps, RouteProps, RoutesProps, To } from "react-router";
export interface LinkProps {
    to: To;
    replace?: boolean;
    state?: any;
    reloadDocument?: boolean;
    children?: React.ReactNode;
}
/** The public API for rendering a history-aware `<a>`. */
export declare const Link: React.ForwardRefExoticComponent<LinkProps & React.RefAttributes<HTMLAnchorElement>>;
/** A `<Router>` for use in web browsers. Provides the cleanest URLs. */
export declare function BrowserRouter({ basename, children }: {
    basename?: string;
    children?: React.ReactNode;
}): React.JSX.Element;
